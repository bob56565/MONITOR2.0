# MONITOR Health Platform Environment Variables

# Public Cloudflare Worker URL (required for live analysis)
NEXT_PUBLIC_MONITOR_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# Optional: legacy server-to-server worker call
# MONITOR_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# Optional: Base path for GitHub Pages builds
# NEXT_PUBLIC_BASE_PATH=/MONITOR2.0

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# Optional: Feature Flags
# NEXT_PUBLIC_ENABLE_DEMO_MODE=true
